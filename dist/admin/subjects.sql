CREATE TABLE IF NOT EXISTS subjects_tb (
    sub_id INT PRIMARY KEY AUTO_INCREMENT,
    sub_name VARCHAR(100) NOT NULL,
    sub_code VARCHAR(50) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);